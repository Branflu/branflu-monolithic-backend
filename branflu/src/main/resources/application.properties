tell me steps to follow this is my enviroment variable # Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/branflu
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}

# JPA & Hibernate Configuration
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Server Port (optional)
server.port=8080

# JWT Configuration
jwt.secret=${JWT_SECRET}
instagram.user.id=${INSTA_USER_ID}
instagram.access.token=${INSTA_ACCESS_TOKEN}
INSTA_REDIRECT_URI=https://fde569a0f368.ngrok-free.app/api/instagram/callback
instagram.client.id=${INSTA_CLIENT_ID}
instagram.client.secret=${INSTA_CLIENT_SECRET}



FACEBOOK_REDIRECT_URI= https://native-violently-imp.ngrok-free.app/api/facebook/callback
facebook.client.id=${INSTA_CLIENT_ID}
facebook.client.secret=${INSTA_CLIENT_SECRET}



# Logging Level (optional)
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE


# OAuth Credentials from Google Cloud Console
youtube.client.id=${YOUTUBE_CLIENT_ID}
youtube.client.secret=${YOUTUBE_CLIENT_SECRET}
youtube.redirect.uri=http://localhost:8080/api/youtube/callback
youtube.scope=https://www.googleapis.com/auth/youtube.readonly

# Set logging level (optional)
logging.level.root=INFO
logging.level.com.yourpackage=DEBUG

# Write logs to a file
logging.file.name=logs/branflu.log
logging.file.path= # Leave empty if using name above

# File size and rotation (optional)
logging.file.max-size=10MB
logging.file.max-history=5

spring.security.oauth2.client.registration.google.redirect-uri=http://localhost:8080/api/oauth2/callback
frontend.url=http://localhost:3000
#ngrok http --url=native-violently-imp.ngrok-free.app 8080




